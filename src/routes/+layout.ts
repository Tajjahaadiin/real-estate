// Enable static prerendering for every route by default.
// Individual routes can override this in their own +layout.ts/+page.ts.
export const prerender = true;
export const trailingSlash = 'always';
